# Shell Script Verification Pipeline
description: Shell script projects

steps:
  - name: lint
    command: shellcheck *.sh **/*.sh 2>/dev/null || shellcheck *.sh
    timeout: 30
    quick: true

  - name: syntax
    command: bash -n *.sh
    timeout: 10
    quick: true
